# Use Python 3.10 for the group-average ENIGMA forecasting workflow.
# The current local myenv uses Python 3.13 and is kept only as a notebook runtime.
jupyterlab>=4.2
ipykernel>=6.29

# Core data processing and modelling
numpy>=1.24
pandas>=1.5
scipy>=1.10
pyyaml>=6.0
scikit-learn>=1.2
pymc>=5.16
arviz>=0.17

# Reporting and notebook figures
matplotlib>=3.7
seaborn>=0.13

# ENIGMA Toolbox dependencies
nilearn>=0.10
vtk>=9.2,<9.4
nibabel>=5.0
pillow>=10.0

# Tests
pytest>=8.0

# ENIGMA Toolbox is not distributed as a normal PyPI package.
enigmatoolbox @ git+https://github.com/MICA-MNI/ENIGMA.git@v2.0.3
